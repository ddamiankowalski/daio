export interface IDaioWidget {
    title: string;
    subtitle: string;
    description: string;
    image: string;
    yImagePos?: string;
}

export interface IDaioGradientWidget {
    title: string;
}